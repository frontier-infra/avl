# AVL Manifest
# Discovery file for AI agents. Like robots.txt but for agent-native views.

version: 1
content-type: text/agent-view; version=1

# Discovery mechanisms supported
discovery:
  - accept-header   # Accept: text/agent-view
  - suffix          # GET /<route>.agent
  - native          # GET /agent/<route>
  - html-link       # <link rel="alternate" type="text/agent-view">

# Auth — the agent inherits the host app's session (delegate, not principal)
session:
  mechanisms: [cookie, bearer]
  cookie: session_token
  bearer_prefix: Bearer
  scope: per-user

# Routes (pattern, method, public-or-auth)
routes:
  - GET /dashboard.agent
  - GET /journey/{id}.agent

# Concrete agent-view inventory for provenance-gated fetchers
agent_views:
  - GET /.agent
  - GET /dashboard.agent
  - GET /journey/J-101.agent
  - GET /journey/J-102.agent
  - GET /journey/J-103.agent

# Fetcher hints
fetcher_hints:
  safe_methods: [GET, HEAD]
  body_link: true
  head_link: true
  conditional_requests: false

# Spec
spec: /specs/avl-agent-view-layer.md
contact: agent-views@argentos.ai
